FROM node:22-alpine

WORKDIR /usr/src/frontend
# Set port to 80, same as production-ready nginx server
ENV PORT=80

COPY ./package*.json ./
RUN npm install

CMD ["npm", "start", "--disableHostCheck", "true"]